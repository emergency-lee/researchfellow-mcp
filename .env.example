# Comma-separated API keys that unlock `full` mode (PR-2 API-key path).
# Absent/unknown keys are NOT rejected — they degrade to free/teaser (§0-2).
RF_API_KEYS=

# Optional NCBI E-utilities API key (higher PubMed rate limit). Works without it.
NCBI_API_KEY=
